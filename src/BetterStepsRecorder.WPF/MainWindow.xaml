<mah:MetroWindow x:Class="BetterStepsRecorder.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utilities="clr-namespace:BetterStepsRecorder.WPF.Utilities"
        xmlns:local="clr-namespace:BetterStepsRecorder.WPF"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        Dialog:DialogParticipation.Register="{Binding}"
        mc:Ignorable="d"
        DataContext="{utilities:DependecyInjection Type=local:MainWindowViewModel}"
        Title="Better Steps Recorder"
        ShowIconOnTitleBar="True"
        Height="650"
        Width="1200">
    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <!-- Setting a Margin and enable hight-quality image -->
            <Image Margin="4"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Source="/Resources/logo.ico" />
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File" IsEnabled="False">
                    <MenuItem Header="_New" />
                    <MenuItem Header="_Open" />
                    <MenuItem Header="_Save As" />
                    <Separator />
                    <MenuItem Header="_Export" >
                        <MenuItem Header="_To File">
                            <MenuItem Header="_RTF"></MenuItem>
                            <MenuItem Header="_HTML"></MenuItem>
                            <MenuItem Header="_ODT"></MenuItem>
                        </MenuItem>
                        <MenuItem Header="_To Obsidian Vault"/>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="_Help" Command="{Binding OpenHelpDialog}"/>
            </Menu>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="250"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                
                <ListBox Grid.Row="0" BorderBrush="{StaticResource MahApps.Brushes.Accent3}" BorderThickness="1" Margin="5,0">
                    <ListBoxItem Content="Test1"></ListBoxItem>
                    <ListBoxItem Content="Test2"></ListBoxItem>
                    <ListBoxItem Content="Test3"></ListBoxItem>
                </ListBox>
                <Button Margin="5" Grid.Row="1" Command="{Binding StartRecording}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="16" Height="16" Fill="Red"/>
                            <Label Content="Start Recording" FontSize="12"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </Grid>
            <GridSplitter Grid.Column="1" Width="3"></GridSplitter>
            <Grid Grid.Column="2">
                <Image Source="/Resources/logo.ico" />
            </Grid>
            <GridSplitter Grid.Column="3" Width="3"></GridSplitter>
            <Grid Grid.Column="4">
                
            </Grid>
        </Grid>
    </Grid>
</mah:MetroWindow>
