<mah:MetroWindow x:Class="BetterStepsRecorder.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BetterStepsRecorder.WPF"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        DataContext="{local:DependecyInjection Type=local:MainWindowViewModel}"
        Title="Better Steps Recorder"
        ShowIconOnTitleBar="True"
        Height="650"
        Width="1200">
    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <!-- Setting a Margin and enable hight-quality image -->
            <Image Margin="4"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Source="/Resources/logo.ico" />
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File" IsEnabled="False">
                    <MenuItem Header="_New" />
                    <MenuItem Header="_Open" />
                    <MenuItem Header="_Save As" />
                    <Separator />
                    <MenuItem Header="_Export" >
                        <MenuItem Header="_To File">
                            <MenuItem Header="_RTF"></MenuItem>
                            <MenuItem Header="_HTML"></MenuItem>
                            <MenuItem Header="_ODT"></MenuItem>
                        </MenuItem>
                        <MenuItem Header="_To Obsidian Vault"/>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="_Help"/>
            </Menu>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="250"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
            <Label Content="Steps"/>
        </Grid>
        <GridSplitter Grid.Column="1" Width="3"></GridSplitter>
        <Grid Grid.Column="2">
            <Label Content="Screenshot preview"/>
        </Grid>
        <GridSplitter Grid.Column="3" Width="3"></GridSplitter>
        <Grid Grid.Column="4">
            <Label Content="Properties"/>
        </Grid>
        </Grid>
    </Grid>
</mah:MetroWindow>
